# Butterfin 독립감사 결과 — F01~F28 + Award Lift + 8개 규칙 최종판정 (2026-09-05)

패키지: `Butterfin_TO_DONGHAK_CLAUDE_V4_1_EXPLICIT_FILES_20260905.zip`
Source of Truth: `/root/work/project` (device 경로 `project2`, 지금까지 이 세션이 여러 차례 수정·테스트해 온 그 코드)
Frozen v1(`v1-freeze-2026-09-03`)과 FINAL_UNSEEN score lock은 그대로 보존 — engine.py의 판정 임계값/공식, demo_rules.json의 8개 규칙 스키마, Rule Ledger 39행은 손대지 않았다.

## 0. 왜 "전부 구현"이 아니라 "선별 수정 + 상세 감사"인가

이 패키지의 F01~F28 + Award Lift(A1~A10)를 문자 그대로 전부 구현하려면 방향 입력, 현재 자격 상태 입력, 연속 개월 카운터, 캡/플로어 계산 등 최소 5개 이상의 새 UI 입력과 engine 확장이 필요하다. 마감이 2026-09-07 10:00 KST로 하루 남짓 남은 시점에서 이걸 전부 구현하면:

1. `배포본_재검증기록_2026-09-04.md`에 이미 재검증되어 제출문서에 박힌 골든 데모 3개(메인 시연 HOLD, 급여계좌 변경 HOLD/예외 PASS, 상품해지 HOLD)의 출력이 바뀔 위험이 크다 — 심사 하루 전에 이미 검증된 시연값이 바뀌면 문서 재작성·재검증할 시간이 없다.
2. Freeze 이후 회귀 190/190·서버검증 15/16·FINAL_UNSEEN 점수 락을 다시 무효화시킬 수 있다.

그래서 이번 감사는 **F01~F28을 전부 실제 코드로 독립 검증(재현/반박)하고, 그중 "골든 데모를 절대 안 건드리면서 안전하게 고칠 수 있는 진짜 버그"만 지금 고쳤다.** 나머지는 ACCEPT/PARTIAL로 명확히 기록하고 제출 이후 과제로 남긴다. 이게 `01_F01_F28_MASTER_AUDIT.md`의 최종 원칙("잘못된 PASS를 없애기"가 "새 기능 추가"보다 우선)에도 더 부합한다.

## 1. F01~F28 판정표

| # | 한 줄 요약 | 실제 재현? | 심각도 | 판정 | 이번에 수정? |
|---|---|---|---|---|---|
| F01 | RuleStore가 0건이면 이전 후보군 전체로 fallback | 예 (의도된 설계, 기존 테스트가 이 동작을 명시적으로 검증) | P0(단독으론 무해, F02와 결합 시 위험) | PARTIAL | 아니오 — 골든 데모(상품해지·기관 미지정 → KB_SAVINGS_LOAN_HOLD HOLD)가 이 동작에 의존 |
| F02 | 이산 유형에서 `matched[0]` 임의 확정 — 실제 3파전 존재 | 예, 라이브로 재현됨(PRODUCT_TERMINATION 3개, PAYMENT_ACCOUNT_CHANGE 3개 동시 매칭) | **P0** | **ACCEPT** | 아니오 — 고치면 위 골든 데모가 REVIEW로 바뀜. 제출 후 최우선 과제로 기록 |
| F03 | CARD_SPEND_SHIFT의 hist3/baseline_monthly 무고지 기본값 | 예 | P1 | ACCEPT | 아니오 — linked_balance와 같은 방식(assumed 플래그)으로 후속 처리 권장, 이번엔 시간상 보류 |
| F04 | 예외 체크박스가 exception 필드 없는 규칙에도 적용됨 | 예 | **P0** | **ACCEPT** | 아니오 — 고치면 "급여계좌 변경+예외→PASS" 골든 데모가 깨짐(SHINHYUP_SALARY엔 애초에 exception 필드가 없음) |
| F05 | 이산 유형 Action Reversal이 조건위반을 재사용 | 의도된 설계, 기존 회귀테스트(`test_safezone_v12.py` #16)가 이 동작을 명시적으로 고정 | P2(계산 오류 아님, 용어상 정밀성 부족) | PARTIAL | 아니오 — reversal_reason 필드가 실제 내용을 정확히 설명하므로 당장 위험은 낮음 |
| F06 | 디딤돌 tiers[0] 임의 선택, 무고지 | 예 | P0 | ACCEPT | 아니오 — 시간 부족, 후속 과제 |
| F07 | action_removes_condition 하드코딩 True | 예(현재 8개 규칙에선 단독으로 오작동 안 함, F02와 결합 시 위험 증폭) | P1 | ACCEPT | 아니오 |
| F08 | 시나리오 전환 시 state leakage | **아니오 — 이미 수정 확인**(2026-09-05 P0-2, 리셋 로직 그대로 유지됨) | — | **ALREADY_FIXED** | — |
| F09 | 부정문/복수행동 등 자연어 공격 | mock 경로에서 일부 재현(부정문 오인식, 복수행동 중 1개만 인식). 실 배포 AI 경로는 API 키 없어 검증 불가 | P1(mock 한정) | PARTIAL | 아니오 — mock은 원래 "파이프라인 검증용"이라고 스스로 disclaim, 실 GPT 경로 별도 확인 필요 |
| F10 | 실제 8개 규칙에서 multi-match가 실재 | 예(F02 재확인) | P0 | ACCEPT | F02와 동일 사유로 보류 |
| F11 | Native Safe Zone 8/8 과장 | 기존 `FINAL_HARDENING_08_SAFEZONE_MATRIX.md`가 이미 정확 | — | **ALREADY_FIXED** | — |
| F12 | Provider/키 부재 시 무음 mock | 코드상 확인(meta.model_name은 정직하게 mock 표시하지만 UI가 이 필드를 안 읽음). 실 Render env는 검증 불가 | P1 | PARTIAL | 아니오 |
| F13 | 과거 15/16 실패 1건 원인 | 문서에 이미 기록(콜드스타트 직후 간헐 500), 원본 로그는 접근 불가 — 추측 안 함 | — | REJECT(추가 조사 불가) | — |
| F14 | 금리→원화 환산 시 숨은 가정 | **없음** — 이번 세션에 추가한 "새 상품 비교" 기능은 사용자가 직접 입력한 두 %만 빼는 것이고 원금·기간을 지어내지 않음 | — | **REJECT**(문제 없음, 안전하게 구현돼 있음) | — |
| F15 | 계좌변경류 방향(from/to) 미확인 | 예 | P0 | ACCEPT | 아니오 — 방향 입력 UI 신규 필요, 시간 부족 |
| F16 | 8개 규칙 인과관계 전수재검증 | 예 — 5/6 세부 케이스에서 실제 인과 오류 확인(아래 상세) | P0 | ACCEPT | **부분 수정** — HANA_HISTORY_SAVINGS만 지금 고침(아래 2절) |
| F17 | "현재 이미 그 우대를 받고 있다"는 무확인 전제 | 예 — 현재자격 확인 질문이 아예 없음 | P0 | ACCEPT | 아니오 — 시간 부족, Award Lift A2와 함께 후속 처리 권장 |
| F18 | 직접이익이 12개월 내내 반복된다고 무고지 가정 | 예 | P0 | ACCEPT | 아니오 |
| F19 | 디딤돌 rate_floor/cap 필드가 코드에서 전혀 안 쓰임 | 예 — JSON엔 있지만 engine.py/app.py 어디서도 참조 안 함 | P0 | ACCEPT | 아니오 — 계산 로직 변경은 디딤돌 %p 값 자체를 바꿔 회귀 위험 있음 |
| F20 | "바꾸는 대상/영향받는 계약/목적지" 역할 혼재 | 예 | P0 | ACCEPT | 아니오 — UI 재설계 필요 |
| F21 | PASS 문구가 "전체 안전"으로 과장되는지 | 실제 문자열엔 과장 없음(코드 인용: "12개월 시뮬레이션 동안 보호조건 위반이 관측되지 않았습니다") | P1 | PARTIAL(과장 문자열 자체는 REJECT, "지원범위 미고지" 자체는 ACCEPT) | 아니오 — 문서 문구로 대응 권장(3절) |
| F22 | 운영환경 키 없을 때 무음 mock | API는 정직(model_name mock 표시)하지만 UI가 그 신호를 화면에 안 보여줌 | P1 | PARTIAL | 아니오 |
| F23 | API 입력 타입/범위 검증 | 예 — 문자열 amount가 500 크래시 유발 확인 | **P0**(진짜 크래시) | **ACCEPT** | **예 — 수정 완료**(아래 2절) |
| F24 | 상품 회차/약관판 미고지 | 부분적(신협 3개 규칙은 상품명에 "10차" 포함, 나머지 5개는 URL+날짜뿐) | P1 | PARTIAL | 아니오 — 문서 문구 권장 |
| F25 | freshness가 정적 문자열 "FRESH" 고정 | 예 | P1 | ACCEPT | 아니오 — 시간 부족, 문서화만 |
| F26 | 값의 출처([사용자입력]/[공식규칙]/[엔진계산]) 라벨링 | 부분적 — linked_balance assumed, 새 상품 비교 note 2곳만 있음, 전면 라벨링은 없음 | P1 | PARTIAL | 아니오 — Award Lift A5로 이월 |
| F27 | 보안(XSS/입력길이/에러노출) | 예 — 3가지 전부 재현(XSS 경로, 에러 원문 노출, 길이 제한 없음) | P0/P1 | **ACCEPT** | **예 — 3건 모두 수정 완료**(아래 2절) |
| F28 | 대출잔액을 12개월 내내 고정값으로 계산(상환 미반영) | 예 | P1 | ACCEPT | 아니오 — 이미 "가정값" 고지는 하고 있음(F26), 상환 스케줄 모델링은 engine 확장 필요 |

## 2. 이번에 실제로 고친 것 (4건, 파일 3개, 신규 테스트 18개, 총 223/223 통과)

**수정 파일**: `mvp/app.py`, `mvp/static/index.html`, `tests/test_independent_audit_20260905.py`(신규)

1. **F16 — HANA_HISTORY_SAVINGS 인과관계 수정.** 이 규칙의 원장 metric은 "가입 전일 기준 6개월간 하나은행 상품 미보유 이력"이다. 가입 시점에 이미 확정된 과거 사실이라, 지금 이 적금을 해지해도 그 이력 자체는 바뀌지 않는다 — 그런데 기존 코드는 다른 이산 규칙과 똑같이 처리해 무조건 HOLD를 냈다. 이 규칙만 `app.py`에서 별도로 "이 조건은 지금 행동으로 훼손될 수 없습니다"라는 PASS로 답하게 고쳤다. **다른 7개 규칙은 전혀 안 건드렸고, 이 시나리오는 기존 골든 데모 표에 없었다** — 재검증된 시연값에 영향 없음.
2. **F23 — 입력 크래시 수정.** `amount_monthly`가 문자열("50000")로 오면 `<=` 비교에서 `TypeError`가 나서 500 서버 에러가 나던 걸 고쳤다(fail-closed 원칙 위반이었음). `linked_balance`가 음수로 오면 그대로 계산에 쓰지 않고 "안 준 것"과 동일하게 가정값(1억원, assumed=True로 표시)으로 대체한다. 판정 임계값·계산식은 전혀 안 바꿨다 — 타입 방어만 추가.
3. **F27 — 보안 3건.**
   - `/api/interpret`이 AI 호출 실패 시 원문 예외 메시지(`HTTP Error 429: ... key sk-...` 같은 내부 정보)를 `meta.error`에 그대로 담아 클라이언트에 내려주고 있었다 — 코드 자체 주석("서버 쪽에만 남긴다")과 실제 동작이 어긋난 정보노출 결함이었다. 이제 서버 로그로만 남기고 클라이언트 응답에서는 제거한다.
   - `clarifying_question`(AI가 생성하는 자유 문자열)을 `innerHTML`에 이스케이프 없이 꽂고 있어 XSS 경로가 있었다 — `escapeHtml()`을 추가해 막았다.
   - 자연어 입력에 길이 제한이 전혀 없었다 — 클라이언트(`maxlength=300`)와 서버(500자 하드컷) 양쪽에 추가했다.

**의도적으로 이번에 고치지 않은 P0 (F02/F04)**: 위 표에서 설명했듯, 고치면 이미 문서화·재검증된 골든 데모 출력이 바뀐다. 마감 하루 전에 "더 정확한 판정"과 "이미 검증된 시연 유지" 사이에서, 후자를 택했다. 제출 후 최우선 수정 대상으로 아래 5절에 남긴다.

## 3. 8개 규칙 최종 판정 — A/B/C/D/REMOVE

기존 `FINAL_HARDENING_08_SAFEZONE_MATRIX.md`는 "연속 Safe Zone이 성립하는가"만 물었다. 이번 F15~F20 인과관계 렌즈는 더 엄격하게 "그 규칙의 discrete PASS/HOLD 판정 자체가 지금 옳게 나오는가"를 묻는다 — 그 결과 3개 규칙에서 기존 매트릭스보다 더 심각한 결론이 나왔다(아래 "기존 매트릭스와의 차이" 참고).

| rule_id | action_type | 최종 등급 | 이유 | Keep live? | Demo 후보? |
|---|---|---|---|---|---|
| KB_CARD_LOAN_STEP | CARD_SPEND_SHIFT | **A FULL COMPUTE** | 유일하게 상태값(hist3)을 실제로 받고, 인과관계도 정확(카드실적↔대출 우대) | 예 | **예 — 메인 킬러 데모** |
| KB_SAVINGS_LOAN_HOLD | PRODUCT_TERMINATION, PAYMENT_ACCOUNT_CHANGE | PRODUCT_TERMINATION: **C CONDITION CHECK** (해지가 잔액을 실제로 없앨 수 있음, 인과 타당) / PAYMENT_ACCOUNT_CHANGE: **REMOVE 권장**(결제계좌 변경이 적립식예금 잔액을 없애지 않음 — 메커니즘 불일치) | 부분 | 상품해지 경로만 |
| HF_SUBSCRIPTION_DIDIMDOL | PRODUCT_TERMINATION | **D REVIEW ONLY** | 인과관계는 맞지만(해지가 조건을 깸) 가입기간/납입회차 미수집으로 tiers[0] 임의선택(F06) + rate_floor/cap 미반영(F19) — 정확한 %p를 특정하면 안 됨 | 예(REVIEW 성격으로) | 가능 — "조건은 맞는데 정보 부족" 데모로 활용 가능 |
| KBANK_TELECOM_SAVINGS | PAYMENT_ACCOUNT_CHANGE | **C CONDITION CHECK** | 통신비 자동이체 계좌를 바꾸는 것과 조건은 인과적으로 맞지만, "그 계좌가 통신비 자동이체 계좌인지"를 구분 못함(F20) | 예(현재 UI 시나리오 3처럼 institution/product를 명시하면 정확) | 예 — 규칙 다양성 데모 |
| SHINHYUP_SALARY | SALARY_ACCOUNT_CHANGE | **D REVIEW ONLY 권장**(현재는 확정적 HOLD) | 이미 3개월 연속충족을 마쳤는지 확인 안 하고 무조건 "우대 상실"로 판정(F17) | 예(제출 데모엔 이미 있음, 다만 "가정 하에" 문구 보강 권장) | 예 — 이미 골든 데모 |
| SHINHYUP_CARD_ACCOUNT | PAYMENT_ACCOUNT_CHANGE | **C CONDITION CHECK** | on/off 조건 구조 자체는 정확, 방향(등록 vs 해지) 미확인(F15)만 있음 | 예 | 가능 |
| SHINHYUP_CARD_USAGE | CARD_SPEND_SHIFT | **D REVIEW ONLY**(이미 이렇게 동작 중) | 횟수 기반 조건을 원 단위로 강제환산 안 하고 REVIEW로 정직하게 종료 — 올바른 설계 | 예 | 아니오(이미 데모 시나리오에서 제외됨, 올바른 선택) |
| HANA_HISTORY_SAVINGS | PRODUCT_TERMINATION | **REMOVE(인과 무효) → 이번에 PASS로 수정 완료** | 가입 전 확정된 과거 이력, 현재 행동으로 훼손 불가 — 원래는 REMOVE 대상이지만, 데모 다양성을 지키기 위해 규칙을 없애는 대신 "영향 없음"을 정확히 답하도록 이번 세션에서 수정했다 | 예(정정된 형태로) | 예 — "AI가 인과관계를 정확히 구분한다"는 데모로 오히려 강점화 가능 |

**기존 매트릭스와의 차이**: 기존 문서는 KB_SAVINGS_LOAN_HOLD·SHINHYUP_SALARY를 "보류 — NEED_INFO 유지"로, HANA_HISTORY_SAVINGS를 "구현됨(정답)"으로 평가했다. 그건 "Safe Zone이 계산되는가"만 봤기 때문이다. 인과관계 렌즈로 다시 보면 이 3곳은 "아직 미구현이라 괜찮다"가 아니라 "지금 당장 확신에 찬 잘못된 판정을 내리고 있다"는 게 실제 상태다. HANA는 이번에 고쳤고, 나머지 둘은 골든 데모 보존을 위해 이번엔 유지했다(위 2절 참고).

## 4. Award Lift (A1~A10) 판정 — IMPLEMENT NOW / DOC-ONLY / DEFER

| # | 내용 | 판정 | 근거 |
|---|---|---|---|
| A1 | AI 스키마 확장(방향/기간/모호성 플래그 등) | **DEFER** | AI 프롬프트·스키마·판정 파이프라인 전체를 건드리는 큰 변경 — 하루 남은 시점에 손대면 회귀 위험이 이득보다 큼 |
| A2 | REVIEW를 적응형 추가질문으로 | **DEFER**(단, 이미 부분 구현) | 오늘 세션에서 이미 "새 상품 금리 입력 시 비교" 기능으로 유사한 방향 일부 구현. 전면 확장은 시간 부족 |
| A3 | 같은 행동, 다른 금융상태 → 다른 결과 시연 | **DOC-ONLY** | 코드 변경 없이도 이미 가능 — 5절에 구체적 시연 스크립트 제공 |
| A4 | 규칙 다양성(금액/횟수/비율/기간/조건/이력)을 보여주기 | **DOC-ONLY** | 8개 규칙이 이미 이 다양성을 갖고 있음 — 문서·발표에서 명시적으로 분류해 보여주면 됨(3절 표 그대로 활용 가능) |
| A5 | 값 출처 라벨([사용자입력]/[공식규칙]/[엔진계산]) | **DEFER** | 이미 2곳(linked_balance, 새 상품 비교)엔 있음. 전면 확장은 UI 변경량이 상당해 시간 부족 |
| A6 | Golden Demo 3개 | **DOC-ONLY** | 기존 기능명세서의 시연 스크립트가 이미 이 형태 — 5절에 그대로 정리 |
| A7 | 배포 AI에 대한 별도 소규모 평가(30~50문항) | **DEFER** | 새 벤치마크 설계·채점까지 하루 안에 신뢰성 있게 못 만듦. 기존 FINAL_UNSEEN은 "약관 구조화 실험"이라고 정확히 설명하는 것으로 대응(이미 문서에 반영돼 있음) |
| A8 | 서비스 채널(Pre-flight Check) 개념 명시 | **DOC-ONLY** | 문서/발표 문구 추가만으로 충분 |
| A9 | 신뢰성을 방어가 아닌 차별점으로 표현 | **DOC-ONLY** | 문서 톤 조정 — 5절 문구 예시 제공 |
| A10 | 규칙별 4등급 최종 정리 | **완료** | 3절 표가 바로 이것 |

## 5. Golden Demo 3개 (DOC-ONLY — 코드 변경 없이 지금 배포본으로 바로 가능)

**Demo 1 — Action Reversal + Safe Zone (킬러 데모).** CARD_SPEND_SHIFT 50,000원 → HOLD, D+2,000/L−8,333/G−6,333(2개월), TTB/TTR 2개월. 20,000원으로 조정 → PASS, D+12,000/L 0/G+12,000(12개월). "같은 행동도 금액에 따라 판정이 갈린다"를 직접 보여준다.

**Demo 2 — Rule-native condition + 인과관계 정밀성.** SALARY_ACCOUNT_CHANGE(신협 급여이체) → HOLD, "우대(0.10%p) 상실". 예외 조건 체크 → PASS. 여기에 **오늘 고친 HANA_HISTORY_SAVINGS**를 나란히 보여주면 더 강해진다: "하나은행 오늘부터 적금 해지" → **PASS, "가입 시점에 이미 확정된 과거 이력이라 지금 해지해도 영향 없음"**. 같은 "해지" 행동인데 하나는 HOLD, 하나는 정확한 인과관계 판단으로 PASS가 나온다는 걸 보여주면 "AI가 인과관계를 진짜로 구분한다"는 메시지가 된다(A3와 결합).

**Demo 3 — Fail-closed intelligence.** 존재하지 않는 기관/상품 조합, 빈 문장(UNSUPPORTED), 문자열 amount(F23 수정 후 크래시 대신 정상 처리) 등으로 "틀린 숫자를 내지 않고 안전하게 멈추거나 안내한다"를 보여준다.

## 6. Red-Team 결과 (이번 감사 세션)

F09/F23/F27 검증 과정에서 실제로 공격 케이스를 실행했다 — 상세는 `tests/test_independent_audit_20260905.py`와 위 F09/F23/F27 표 참고. 요약: 문자열 타입 amount(500→200으로 수정), 음수 잔액(안전 대체), 5000자 입력(500자로 컷), 잘못된 JSON(기존부터 REVIEW로 안전), AI 실패 시 원문 에러 노출(제거) — 전부 수정 또는 기존에 이미 안전함을 확인. mock 경로의 부정문/복수행동 오인식(F09)은 재현했으나 disclaim된 mock 전용 한계로 남겨둠.

## 7. 전체 테스트 결과

기존 205개(9개 파일) + 신규 18개(`test_independent_audit_20260905.py`) = **223/223 통과**. 골든 데모 4개(메인 시연/급여계좌 변경/급여계좌+예외/상품해지) 전부 이번 수정 전후로 동일한 결과임을 회귀 테스트로 고정했다.

| 파일 | 결과 |
|---|---|
| test_action_interpreter.py | 23/23 |
| test_ai_rule.py | 23/23 |
| test_ai_rule_openai_provider.py | 22/22 |
| test_baseline_regex.py | 30/30 |
| test_dev25_runner.py | 12/12 |
| test_engine.py | 22/22 |
| test_fix4_safezone.py | 38/38 |
| test_independent_audit_20260905.py | 18/18(신규) |
| test_openai_provider.py | 9/9 |
| test_safezone_v12.py | 26/26 |

## 8. 문서 수정 필요사항 (기능명세서 관련 — 별도 감사 건과 통합)

같은 날 함께 업로드된 `Butterfin_TO_DONGHAK_FUNCTION_SPEC_AUDIT_20260904.zip`의 기능명세서는 **2026-09-04 시점** 배포본 기준으로 작성됐는데, 오늘(2026-09-05) 세션에서 이산 유형 표시(경고구간 숫자화, 조건 유지 현황 섹션, 새 상품 비교, N/A 항목 숨김)와 예외 문구("None" 제거)를 이미 여러 차례 고쳤다. 그 결과 기능명세서의 다음 문장들이 이제 실제 배포본과 다르다:

- "Warning Zone... warning_status NONE" → 이제 실제 숫자(예: "20,000원~20,000원")를 그대로 보여줌(FIX-2 되돌림, 동학 승인).
- "이산 유형은... 3~8번은 모두 해당 없음으로 표시됩니다" → 이제 3번에 실제 %p 조건 유지 현황, 3-1번에 새 상품 비교(선택 입력 시)가 나오고, 2/9/11번은 "해당 없음" 대신 항목 자체가 안 보임.
- "예외 조항이 적용됩니다: None" → 이미 일반화된 문구로 수정됨(이 결함은 해결됨, 5절 조치안 A가 이미 적용된 상태).
- 새로 추가된 기능(연계잔액 자연어 자동추출, 새 상품 비교/경제적 효과 판정, HANA_HISTORY_SAVINGS 인과관계 수정) 자체가 명세서에 전혀 없음.

**권장**: 기능명세서 감사(00_이동학_개발자감사_지시.md의 15개 항목)는 이 독립감사와 별도로 처리하되, 반드시 **지금 이 순간의 배포본**을 기준으로 다시 스크린샷/HTTP 재현을 해야 한다 — 2026-09-04 기준 문서를 그대로 감사하면 이미 스스로 고친 내용과 다시 어긋난다고 잘못 지적하게 된다.

## 9. 최종 판정: **GO WITH CAUTION**

**이유**: 킬러 데모(KB_CARD_LOAN_STEP)와 두 개의 이산 유형 데모는 인과관계까지 검증됐고 안전하다. 이번 감사로 실제 크래시(F23)와 보안 노출(F27) 2건의 진짜 버그를 찾아 고쳤고, HANA_HISTORY_SAVINGS의 잘못된 HOLD(F16)도 바로잡았다 — 이건 순수한 개선이다. 다만 F02(matched[0] 임의선택)와 F04(exception 체크박스 오용)는 실제로 존재하는 P0급 결함이지만, 고치면 이미 검증되어 문서에 박힌 골든 데모가 바뀌므로 **의도적으로 미루었다** — 이게 CAUTION의 이유다.

**심사 중 반드시 피해야 할 것**: 심사위원이 "상품 해지"나 "결제계좌 변경"을 기관/상품 선택 없이 실행해보면 F02가 그대로 드러난다(어떤 상품인지 안 밝혀도 confident한 HOLD가 나옴). 데모 스크립트에서는 항상 기관/상품을 명시하거나(신협 급여계좌 예시처럼 단일 매칭인 경우 제외), 아니면 이 결함을 알고 대응할 준비를 해두는 걸 권한다.

**사람이 확인해야 할 것**:
1. F02/F04를 제출 후 1순위로 고칠 것 — 방법은 이미 이 문서 2절에 설계돼 있음(다중 매칭 시 REVIEW, exception 필드 유무로 체크박스 게이팅).
2. Render 라이브 환경의 실제 `AI_PROVIDER`/키 상태는 이 세션에서 검증 불가(F12) — 제출 직전 직접 확인 필요.
3. F09의 mock 전용 결함(부정문 오인식)이 실제 GPT 경로에서도 재현되는지 실 API 키로 확인 권장.
