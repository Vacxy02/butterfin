# V7_REMAINING_LIMITATIONS.md

이번 V7 패스가 "일부러 하지 않은 것"과 "이 세션이 원천적으로 확인할 수 없는 것"을
구분해서 남긴다. `01_FINAL_7_FIXES.md`의 "절대 하지 말 것" 목록을 지키기 위해
의도적으로 손대지 않은 부분이 대부분이다.

## 의도적으로 하지 않은 것 (스펙의 "절대 하지 말 것" 준수)

- Safe Zone(Robust/Warning/Nominal, TTB/TTR, Financial Cliff)을 CARD_SPEND_SHIFT
  외 다른 7개 규칙으로 확장하지 않음.
- N-차원/Joint Action(여러 행동을 동시에 평가) 기능을 추가하지 않음.
- 모든 규칙에 공통 적용되는 범용 잔액/기간/반올림 입력 UI를 새로 만들지 않음.
- 대출 상환 스케줄, 적금 세금/중도해지 현금흐름 등 새 금융 모델링을 추가하지 않음.
- LLM 스키마(action_interpreter의 구조화 항목)를 손대지 않음.
- 새로운 추천/제안 기능을 추가하지 않음.
- FINAL_UNSEEN 재실행이나 그 점수에 영향을 주는 threshold/effect 값 튜닝을 하지
  않음(demo_rules.json 자체는 이번 V7 범위에서 값 하나도 바꾸지 않았다).

## 디딤돌대출 구간(tier) 판정의 남은 한계

`enrollment_years`/`payment_count` 중 하나라도 사용자가 준 경우, 이번 V7은 그
값으로 실제 어느 구간(tier)인지 골라 %p를 계산하는 로직까지는 새로 만들지
않았다 — "정보가 없을 때 임의로 tiers[0]을 고르지 않는다"(fail-closed)까지만
구현했다. 실제 구간별 계산 로직 구현은 `01_FINAL_7_FIXES.md`가 지정한 7개
항목에 포함되지 않았으므로 이번 범위 밖으로 남겨둔다.

## 이 세션이 원천적으로 확인할 수 없는 것

- 실제 배포된 `https://butterfin.onrender.com`에서의 렌더링 — `V7_LIVE_SMOKE.md`
  참조. 로컬 Flask 테스트 클라이언트로만 검증했다.
- 실제 브라우저(Chrome 데스크톱/모바일)에서 새로 추가한 화면 문구·후보 카드
  UI가 레이아웃 깨짐 없이 보이는지.
- 실제 OpenAI API 키가 걸린 프로덕션 `/api/interpret` 동작(이 환경엔 키가 없어
  mock/에러-주입 테스트로만 커버).
- 사용자(동학님)가 실제로 화면을 눌러보며 "이게 진짜 원하던 표시 방식이 맞는지"
  — 이건 코드/테스트로 검증할 수 있는 영역이 아니라 사람 확인이 필요한 부분이다.

## 알려진 설계상의 트레이드오프 (버그 아님, 의도된 선택)

- 이산형(해지/계좌변경) 판정의 REVIEW 응답 중 일부(규칙 불일치, 다중매칭)는
  `effects`/`safety`/`condition` 키 자체가 없다 — 화면(index.html)이 이를
  감지해 상세 섹션 대신 후보 목록만 보여주도록 방어 코드를 추가했다. 이건 API
  응답 형태를 굳이 통일하지 않고 "계산 안 한 건 계산 안 했다고 표시"하는 기존
  원칙을 그대로 따른 것이다.
- HANA_HISTORY_SAVINGS를 PASS로 유지하는 결정은 "가입 전 이력 조건은 현재
  행동으로 훼손 불가"라는 인과관계 판단에 근거한다 — 이 판단 자체가 맞는지는
  이 규칙의 원문 근거(evidence)를 사람이 다시 한번 확인하는 게 안전하다.
